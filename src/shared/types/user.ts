export interface UserInfo {
  name: string;
  avatarUrl: string;
  isPro: false;
  email: string;
  token: string;
}

export interface FeedbackInfo {
  offerId: string;
  comment: string;
  rating: number;
}
